1. npm init -y
2. npm install express socket.io moment(momentjs used to format date and time)
3. npm install -D nodemon(dont need to restart server)



https://cdnjs.com/libraries/qs cdn to get the query value of url